const sendMail = require("./sendMail");
const createError = require("./createError");
const schemaSelect = require("./schemaSelect");

module.exports = {
  sendMail,
  createError,
  schemaSelect,
};
